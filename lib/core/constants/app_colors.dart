import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Màu chính của thương hiệu (ví dụ shop cầu lông)
  static const Color primary = Color(0xFF1E88E5); // Xanh dương chủ đạo
  static const Color primaryDark = Color(0xFF1565C0); // Xanh đậm hơn
  static const Color accent = Color(0xFFFFC107); // Vàng nhấn

  // 🎨 Màu nền
  static const Color background = Color(0xFFF5F5F5); // Xám nhạt
  static const Color scaffold = Colors.white; // Trắng cho Scaffold

  // 🎨 Màu text
  static const Color textPrimary = Color(0xFF212121); // Đen đậm
  static const Color textSecondary = Color(0xFF757575); // Xám chữ phụ
  static const Color textthird = Colors.white; // Xám chữ phụ

  // 🎨 Màu trạng thái
  static const Color success = Color(0xFF4CAF50); // Xanh lá thành công
  static const Color error = Color(0xFFF44336); // Đỏ lỗi
  static const Color warning = Color(0xFFFF9800); // Cam cảnh báo

  // 🎨 Màu viền hoặc divider
  static const Color border = Color(0xFFE0E0E0); // Xám viền
}
